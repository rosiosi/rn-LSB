class Sign {
    constructor(id, title, categoryId, imagePath, videoPath, points) {
        this.id = id;
        this.title = title;
        this.categoryId = categoryId;
        this.imagePath = imagePath;
        this.videoPath = videoPath;
        this.points = points;
    }
}

export default Sign;